node_modules/
*.tsbuildinfo
.env
.env.*
coverage/

# tsc CLI output — published via npm publish, not committed to git
lib/

# ncc Action bundle — dist/index.js is committed so GitHub can run it without npm install
dist/*
!dist/index.js
!dist/index.js.map
!dist/sourcemap-register.js
