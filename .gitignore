node_modules/
*.tsbuildinfo
.env
.env.*
coverage/

# tsc output — published via npm, not committed to git
# ncc Action bundle (dist/index.js etc.) is committed so GitHub can run it
dist/*
!dist/index.js
!dist/index.js.map
!dist/sourcemap-register.js
