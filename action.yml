name: 'AgentGate - AI PR Guardrails'
description: 'Guardrail checks for AI-agent-generated pull requests'
author: 'Brett Buskirk'

inputs:
  github-token:
    description: 'GitHub token for API access'
    required: true
    default: ${{ github.token }}
  config-path:
    description: 'Path to .agentgate.yml config file'
    required: false
    default: '.agentgate.yml'
  fail-on:
    description: 'Minimum severity that fails the check (error | warning | never). Overrides config file.'
    required: false
  anthropic-api-key:
    description: 'Anthropic API key for the optional LLM intent check (or set the ANTHROPIC_API_KEY env var).'
    required: false

outputs:
  verdict:
    description: 'Overall verdict: pass | warn | fail'
  finding-count:
    description: 'Total number of findings'

runs:
  using: 'node20'
  main: 'dist/index.js'

branding:
  icon: 'shield'
  color: 'blue'
